syntax = "proto3";
option java_package = "orbit.shared.proto";

package orbit.shared;

service Connection {
    rpc OpenMessageStream (stream MessageStreamRequest) returns (stream MessageStreamResponse) { }
}

message MessageStreamRequest {
    string address = 1;
    string message = 2;
}

message MessageStreamResponse {
    string message = 1;
}
